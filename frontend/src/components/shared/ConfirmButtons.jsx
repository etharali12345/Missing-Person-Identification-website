/*
const ConfirmButtons = ({ matchId, onDecision }) => {
  return (
    <div>
      <button onClick={() => onDecision(matchId, "confirmed")}>Confirm</button>

      <button onClick={() => onDecision(matchId, "rejected")}>Deny</button>
    </div>
  );
};
*/
